using System;
using System.Collections.Generic;
using System.Text;

namespace LDToypad
{
    public enum MinifigType
    {
        Unrecognized = -1,
        Unknown = 0,
        Character = 1,
        Vehicle = 2
    }
}
